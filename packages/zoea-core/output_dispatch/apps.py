"""Django app configuration for output_dispatch."""

from django.apps import AppConfig


class OutputDispatchConfig(AppConfig):
    """Configuration for the output_dispatch app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "output_dispatch"
    verbose_name = "Output Dispatch"
