[project]
name = "zoea-vm"
version = "0.1.0"
description = "VM creation, management, and deployment tools (placeholder)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.uv]
package = true
